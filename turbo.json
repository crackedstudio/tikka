{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalDependencies": ["tsconfig.base.json", "pnpm-lock.yaml", ".nvmrc", "package.json"],
  "globalEnv": ["NODE_ENV", "CI"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tsconfig*.json", "nest-cli.json", "vite.config.*", "package.json"],
      "outputs": ["dist/**"],
      "env": ["ANALYZE", "VITE_*"]
    },
    "lint": {
      "inputs": ["src/**", "*.config.*", "eslint.config.*", "package.json"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**", "tests/**", "jest.config.*", "vitest.config.*", "package.json"],
      "outputs": ["coverage/**"],
      "env": ["NODE_ENV"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tsconfig*.json", "package.json"],
      "outputs": []
    },
    "docs": {
      "dependsOn": ["build"],
      "inputs": ["src/**", "typedoc.json", "package.json"],
      "outputs": ["docs/api/**"]
    }
  }
}
