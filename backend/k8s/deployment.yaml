apiVersion: apps/v1
kind: Deployment
metadata:
  name: tikka-backend
  namespace: tikka
  labels:
    app: tikka-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tikka-backend
  template:
    metadata:
      labels:
        app: tikka-backend
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: tikka-backend
          image: ghcr.io/crackedstudio/tikka-backend:latest
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: tikka-backend-config
            - secretRef:
                name: tikka-backend-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
