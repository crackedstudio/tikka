# Secrets are base64-encoded. Generate with:
#   echo -n "your-value" | base64
#
# Do NOT commit real values. Use a secrets manager (e.g. Sealed Secrets, ESO) in production.
apiVersion: v1
kind: Secret
metadata:
  name: tikka-indexer-secrets
  labels:
    app: tikka-indexer
type: Opaque
data:
  DATABASE_URL: <base64-encoded-database-url>
  DATABASE_REPLICA_URL: <base64-encoded-replica-url>   # optional
  TIKKA_CONTRACT_ID: <base64-encoded-contract-id>
